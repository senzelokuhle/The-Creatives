@model ModelsLogic.Gender

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Gender</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model._Gender)
        </dt>

        <dd>
            @Html.DisplayFor(model => model._Gender)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.GenderId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
